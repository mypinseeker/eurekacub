/**
 * EurekaCub Renderer Registry — Auto-registration entry point
 *
 * Import this file once (e.g., in main.tsx) to register all built-in renderers.
 * Community renderers should be imported separately.
 */

// Built-in module renderers (Iter 1)
import './symmetry'
import './fraction'
import './sequence'
import './probability'
